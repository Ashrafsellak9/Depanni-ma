global:
  scrape_interval: 15s

scrape_configs:
  - job_name: depanni-api
    metrics_path: /metrics
    static_configs:
      - targets: ["host.docker.internal:4000"]

rule_files:
  - /etc/prometheus/prometheus-alerts.yml
