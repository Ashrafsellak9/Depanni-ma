# Sons de notification

Placez ici :

- `notification.mp3` — nouvelle offre standard
- `notification-urgent.mp3` — demande urgente (`urgency=NOW`)

Les fichiers sont optionnels ; l’app fonctionne sans eux (toast uniquement).
